"use strict";

let scores0 = new Array(); // 空数组

let scores0_1 = Array(); // 空数组，使用构造函数可省略 new 

let scores10 = Array(10); // 初始大小为 10

let scores2 =  Array(1, 4, 9, 10); // 包含四个初始元素

let color =  Array("red"); // 单个初始元素

let colors = ['red', 'green', 'blue']; // 使用数组字面量形式创建

console.log(scores2);
